{
  "name": "steppe-strike",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "One persistent multiplayer block world for Mongolia.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "start": "node server/index.js",
    "build": "vite build",
    "test": "node --test test/*.test.js && node test/smoke.js",
    "test:live": "node test/live-smoke.js",
    "check": "npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "three": "^0.179.1",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "vite": "^7.0.6"
  }
}
